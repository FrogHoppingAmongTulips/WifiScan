# WFS — Roadmap улучшений

Колонки: **Усилие** (S/M/L), **Ценность** (⭐–⭐⭐⭐). Веб исключён (только терминал, stdlib, macOS).

## ✅ Сделано в v2.2 (2026-07-02) — полный анализ

| Улучшение | Где |
|---|---|
| **`report`** — единый отчёт: health + устройства + безопасность + качество, вердикт «проблемы/ок» | `do_report` (+ `--report`) |
| **`quality`** — потери/задержка/jitter до шлюза и интернета | `ping_quality`, `do_quality` (+ `--quality`) |
| **`trends`** — присутствие устройств во времени + спарклайн | `store.scan_counts`, `show_trends` (+ `--trends`) |
| Рекомендация лучшего 5GHz-канала | `recommend_channel` в `wifi_audit` |
| Риски по баннерам (дефолт-креды/CVE) | `identify.banner_risk` → детали устройства |
| Health-check сети (шлюз/интернет/DNS) | `net_health` |
| Тесты на новые функции | `tests` (20 тестов) |

## ✅ Сделано в v2.1 (2026-07-02) — инженерный проход

| Улучшение | Где |
|---|---|
| Random-MAC больше не даёт ложный «intruder» и не шумит в NEW | `security.py`, `wifi_cli.main` |
| ARP-spoof детект скоупится по подсети (нет ложной тревоги при смене сети) | `store.check_gateway_mac`, `security.audit` |
| Ограничена конкуренция потоков (fingerprint + enrichment) | `identify.port_fingerprint`, `full_scan` |
| Observability: `--debug`, предупреждения про nmap/интерфейс | `wifi_cli` (logging) |
| Быстрый рескан `rr` (ICMP, ~2с) + дельта зашёл/ушёл | `fast_sweep`, `quick_refresh` |
| Строка NEW показывается один раз (не висит) | `wifi_cli.main` |
| Кэш портов на сессию (`sec`/детали не сканируют дважды детали) | `wifi_cli` port_cache |
| Чистые парсеры `parse_arp_output`/`parse_nmap_output` + тесты | `wifi_cli`, `tests` (16 тестов) |
| Отчёты безопасности — 0600 (только владелец) | `security._write_private` |

## ✅ Сделано в v2.0 (2026-07-02)

| Улучшение | Где |
|---|---|
| ARP-spoof детект (смена MAC шлюза) | `store.py` / `security.py` |
| Дельта портов vs прошлый аудит | `store.py.update_ports` / `security.py` |
| JSON-отчёт безопасности | `security.write_report_json` |
| Авто-загрузка OUI при первом запуске (фон) | `wifi_cli.maybe_auto_oui` |
| CLI-флаги для скриптов/cron (`--scan/--sec/--watch/--history/--export/--json`) | `wifi_cli.run_cli` |
| Экспорт JSON/CSV | `exporter.py`, команда `export`, `--export` |
| Конфиг `~/.wifi-scanner/config.json` | `config.py` |
| Убраны голые `except:` → `except Exception:` | все модули |
| Тесты (14, без сети) + `pyproject.toml` (pipx) | `tests/`, `pyproject.toml` |
| Параллельное сканирование + enrichment | `wifi_cli.full_scan` (v1.5) |
| История/метки/OS-hint/NetBIOS | `store.py`, `identify.py` (v1.5) |

## Осталось (идеи на будущее)

## 🔒 Безопасность

| Идея | Усилие | Ценность | Заметки |
|---|---|---|---|
| ARP-spoof детект | S | ⭐⭐⭐ | сравнивать MAC шлюза `.1` между сканами (данные уже в `store.py`); смена = возможный MITM |
| База дефолт-паролей по баннерам | M | ⭐⭐⭐ | у нас есть `grab_banners`/`guess_from_banners`; добавить «этот баннер → admin:admin / известная дыра» |
| Историческая дельта в `sec` | S | ⭐⭐ | «на устройстве открылся порт 23, которого вчера не было» (сравнение с прошлым сканом из БД) |
| Экспорт отчёта в JSON | S | ⭐⭐ | рядом с текстовым `report-*.txt`, для скармливания другим тулзам |
| Оценка шифрования Wi-Fi | S | ⭐⭐ | флажить WEP/WPA/open в `wifi_audit`,推荐 WPA2/3 |
| Детект «злого двойника» (rogue AP) | L | ⭐⭐ | тот же SSID с другим BSSID в списке соседних сетей |
| Проверка UPnP-экспозиции наружу | M | ⭐⭐ | если роутер отдаёт AddPortMapping — риск |
| DHCP-starvation / новые MAC-флуд алерт | M | ⭐ | много новых MAC за короткое время |

## 🎯 Точность опознания

| Идея | Усилие | Ценность | Заметки |
|---|---|---|---|
| Fingerprint по комбинации портов+mDNS | M | ⭐⭐⭐ | `62078 + _airplay` = точно iPhone, а не просто «Apple» |
| Авто-загрузка OUI при первом запуске | S | ⭐⭐⭐ | сейчас `oui` вручную; тянуть фоном один раз или бандлить компактную базу |
| DHCP hostname sniffing (с sudo) | L | ⭐⭐ | слушать DHCP — устройства сами шлют своё имя |
| Кэш опознания по MAC | S | ⭐⭐ | не переопознавать известные устройства каждый скан → быстрее |
| Больше mDNS/SSDP типов и TXT-полей | S | ⭐ | расширить словари сервисов/брендов |

## ⚙️ Качество кода / рефакторинг

| Идея | Усилие | Ценность | Заметки |
|---|---|---|---|
| Разбить `wifi_cli.py` (1092 стр) на модули | M | ⭐⭐⭐ | `scan.py` (движок), `netinfo.py` (wifi/router/traffic), `ui.py` (меню/вывод), `monitor.py` (watch/uptime) |
| Тесты (pytest) на парсеры | M | ⭐⭐⭐ | mDNS/NetBIOS/nmap/arp вывод → фикстуры без сети; ловит регрессии |
| Тип-хинты везде + mypy | M | ⭐⭐ | сейчас частично; повышает надёжность |
| Единый модуль `net.py` для сокет-хелперов | S | ⭐⭐ | tcp_ping/scan_ports/connect_ex дублируются в 3 местах |
| Убрать `except:` без типа | S | ⭐⭐ | заменить на `except Exception` (голый ловит Ctrl-C) |
| Логирование через `logging` вместо print-отладки | S | ⭐ | опциональный `--debug` |
| Docstrings + `wfs help` с полным описанием команд | S | ⭐ | — |

## 🧭 UX / рабочий процесс

| Идея | Усилие | Ценность | Заметки |
|---|---|---|---|
| CLI-флаги для скриптов/cron | M | ⭐⭐⭐ | `wfs --scan --json`, `wfs --watch`, `wfs --sec` без интерактива |
| Экспорт скана в CSV/JSON | S | ⭐⭐ | команда `export` |
| Конфиг `~/.wifi-scanner/config.toml` | M | ⭐⭐ | свои порты/таймауты/интерфейс (не только en0) |
| Быстрый повторный скан | M | ⭐⭐ | кэш ARP+mDNS между близкими сканами → `r` почти мгновенный |
| Сортировка/фильтр таблицы | S | ⭐ | по IP/типу/риску прямо в меню |
| Поиск устройства | S | ⭐ | `find iphone` |

## ⚡ Производительность

| Идея | Усилие | Ценность | Заметки |
|---|---|---|---|
| Адаптивные таймауты | S | ⭐⭐ | быстрая сеть → короче таймауты пинга/портов |
| Пропускать мёртвые IP в enrichment | S | ⭐⭐ | не делать ttl/netbios/fingerprint для неответивших |
| Тюнинг числа воркеров под размер сети | S | ⭐ | — |
| Инкрементальный watch (только дельта) | M | ⭐ | не полный full_scan каждый раз |

## 💾 Данные / история

| Идея | Усилие | Ценность | Заметки |
|---|---|---|---|
| Таймлайн присутствия устройства | M | ⭐⭐ | «онлайн с 9:00 до 18:00» по данным сканов |
| Заметки к устройству | S | ⭐ | `note 3 роутер соседа` |
| Очистка/экспорт истории | S | ⭐ | `history clear` / `history export` |
| Статистика сети | S | ⭐ | сколько устройств в среднем, пики |

## 📦 Упаковка / распространение

| Идея | Усилие | Ценность | Заметки |
|---|---|---|---|
| `pyproject.toml` + `pipx install wfs` | M | ⭐⭐ | нормальная установка вместо симлинка |
| Homebrew formula | M | ⭐ | `brew install wfs` |
| man-страница | S | ⭐ | — |

## 🖥 Кроссплатформенность (если захочешь уйти от macOS-only)

| Идея | Усилие | Ценность | Заметки |
|---|---|---|---|
| Linux-поддержка | L | ⭐⭐ | `iw`/`nmcli`/`/proc/net/arp` вместо `system_profiler`/`ipconfig` |
| Абстракция «платформенного слоя» | M | ⭐⭐ | интерфейс, реализации per-OS |

---

## Топ-5 по «ценность / усилие» (с чего начать)

1. **ARP-spoof детект** (S, ⭐⭐⭐) — почти бесплатно, реальная security-фича
2. **CLI-флаги + JSON-экспорт** (M, ⭐⭐⭐) — превращает в инструмент для автоматизации
3. **Авто-загрузка OUI** (S, ⭐⭐⭐) — сразу лучше опознаёт вендоров
4. **Разбить `wifi_cli.py` на модули** (M, ⭐⭐⭐) — база для всего остального
5. **Тесты на парсеры** (M, ⭐⭐⭐) — чтобы дальше не ломать

Когда вернёшься — скажи номер, и начнём с него.
